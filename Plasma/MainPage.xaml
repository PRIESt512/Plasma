<Page
    x:Class="Plasma.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Plasma"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:oxy="using:OxyPlot.Windows"
    mc:Ignorable="d">

    <Page.DataContext>
        <local:MainViewModel/>
    </Page.DataContext>
    
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <!--<ListView x:Name="ListView" VerticalAlignment="Top" HorizontalAlignment="Center">
            <DataTemplate>
                <TextBlock>

                </TextBlock>
            </DataTemplate>
        </ListView>-->
        <Button VerticalAlignment="Bottom" HorizontalAlignment="Center" Content="Рассчитать" Click="Button_Click">
        </Button>
        <!--<oxy:PlotView Model="{Binding MyModel}"></oxy:PlotView>-->
    </Grid>
</Page>
